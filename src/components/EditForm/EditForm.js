const EditForm = (props) => {
  return (
    <div>
      <h1>
        <em>edit form will be here soon</em>
      </h1>
      <h1>{props.brewery.name}</h1>
    </div>
  );
};

export default EditForm;
